export class IntervaloSonido {
    public key: string;
    public intervalId: any;
    constructor(key: string, intervalId: any) {
        this.key = key;
        this.intervalId = intervalId;
    }
}